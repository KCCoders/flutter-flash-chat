import 'package:flutter/material.dart';

class FirebaseErrorSnackBar extends StatelessWidget {
  const FirebaseErrorSnackBar({
    Key key,
    @required String errorMsg,
  })  : _errorMsg = errorMsg,
        super(key: key);

  final String _errorMsg;

  @override
  Widget build(BuildContext context) {
    return
  }
}
